# TickerBar 待办清单

> 当前定位：个人可用 Beta。主流程已打通，接下来优先提高长期运行稳定性，再考虑对外分发。

## 2026-08-06：提醒与多股票功能的问题修复

对提醒通知与多股票这批新功能做了第二轮代码审查（Rust 与前端各一轮），按严重度全部修复：

- [x] 配置文件损坏不再导致应用无法启动：损坏文件自动改名备份（config.json.corrupted-时间戳）、回退默认配置继续启动，原因挂在刷新状态/tooltip 上。
- [x] 编辑提醒规则后穿越记忆按「规则指纹」失效：改指标/条件/阈值/股票任一项都重新武装，不再拿旧指标值与新阈值比出假穿越（保存瞬间误弹通知）。
- [x] 通知发送失败（未授权系统通知权限等）记录到刷新状态，不再静默消耗规则；tooltip 可见「提醒通知发送失败」。
- [x] 持仓页改按 symbol 定位编辑对象：删股票后不再静默串到别的股票、覆盖错数据。
- [x] 保存前全量复核提醒规则：关持仓后依赖持仓的规则会被拦下；删股票时联动清理引用它的规则（前端）；后端校验补 id 去重、20 条上限、持仓类指标须有持仓（前后端一致）。
- [x] 刷新防重入：定时循环 / 「立即刷新」/ 保存设置三个触发源互斥，不再并发请求接口或重复评估提醒。
- [x] 托盘渲染 diff：标题/tooltip/菜单行文案与勾选只在真变化时才调用 AppKit setter；update_tray 持读锁渲染不再整份 clone。
- [x] 首启引导真模态化：aria-modal + 背景表单 inert（阻断 Tab 穿透）+ 焦点移入；子分区错误提示补 role="alert"。
- [x] 刷新状态轮询感知窗口可见性：隐藏时暂停，回前台立即拉取并恢复。
- [x] 结构拆分：runtime.rs（1100 行）拆为 runtime/ 子模块；App.vue 抽出 DisplaySection 组件，两边都回到 800 行上限内。
- [x] 「·收/·延」状态后缀默认改为不展示（2026-08-07 决定）：开关本就存在于 设置 → 显示，只翻默认值；「↻/!」故障标志不受影响仍始终显示。

## 2026-08-04：代码审查 + P0 落地

代码审查（Rust 与前端各一轮）后修复，前后端测试、clippy、类型检查全绿：

- [x] 修复 Decimal 溢出崩溃风险：所有外部数值输入限制在 1 万亿以内，配置校验兜底，超大持仓不会再让应用每次启动都崩溃。
- [x] 单个指标不可算（昨收为零、持仓成本为零）时只跳过该指标，不再让整条菜单栏标题渲染失败。
- [x] 刷新循环加自愈：单轮刷新即使 panic 也只损失本轮，循环继续存活。
- [x] 持仓开关关闭再开启时恢复用户原有数量/成本，不再被演示值覆盖。
- [x] 预览请求加防抖与乱序保护，保存操作加重入保护。
- [x] 错误文案统一处理，不再出现 "Error:" 前缀混入中文界面。

## 多股票展示（方案一 · 2026-08-05 完成）

交互方案：菜单栏只显示一只「置顶」股票，点击菜单栏文字弹出下拉列表展示全部股票（名称 + 价格 + 涨跌幅），点击列表行即切换置顶；设置页管理股票列表与逐股持仓。

- [x] 配置结构 v1 → v2：`stocks` 数组 + `activeStock` 置顶下标，持仓归属到单只股票；旧配置文件自动迁移（这也落掉了 P1 的「配置正式迁移机制」首例）。
- [x] 行情批量拉取：腾讯接口一次请求带全部代码，请求压力不随股票数增长；单只解析失败不影响其他股票。
- [x] 菜单栏下拉动态股票列表：勾选标记置顶项，行内展示实时价格与涨跌幅；符号未变时原地更新文字，不打断已展开的菜单。
- [x] 设置页股票列表管理：搜索添加（上限 8 只、去重）、点击置顶、移除（至少保留一只）；持仓页按股票分别配置。
- [x] 保存前批量验证全部股票行情，任一失败即不落盘（延续 P0-1 语义）。
- [x] 市场状态按股票分别推导（A 股与港股时段不同），链路异常（断网等）优先展示。
- [ ] 菜单栏置顶位自动轮播（可选增强，待用户实际使用后决定）。
- [ ] 下拉菜单组合汇总行（属组合管理范畴，暂不计划）。

## 提醒通知（2026-08-05 完成）

需求：价格/涨跌幅/持仓收益（额与率）到达阈值时发送 macOS 系统通知；通知内容可完全自定义为无关文字（伪装用途，如「今天吃了三斤肉」），并支持静默（不响铃）。

- [x] 规则模型：股票 × 4 类指标 × ≥/≤ × 带符号阈值（跌幅/亏损用负数），每条规则可选「每日一次 / 一次性停用」与静默开关。
- [x] 穿越触发语义：上一轮不满足、本轮满足才触发；首次评估只武装不触发；休市/断网数据不参与判定；触发日持久化，重启不补发。
- [x] 自定义通知标题与正文，填写后完全替换默认行情文案，不出现任何股票字眼。
- [x] 配置 v2 → v3（alerts 字段 serde default，旧文件无感升级）；规则引用的股票必须存在，后端校验兜底。
- [x] 设置页「提醒」分区：规则列表（开关/编辑/删除）+ 新建表单（含阈值与持仓依赖校验）。
- [x] 「试发」按钮（2026-08-11）：休市时提醒不判定（不动的收盘价会天天满足条件），无法等它自己响，因此每条规则加试发入口——跳过穿越判定，但文案组装、静默开关与发送路径与真实触发**完全共用** `build_notification`，试发通过即证明通知权限与伪装文案都没问题；失败时给「去系统设置→通知里允许」的可操作提示。
- [ ] 挂机验证触发准确性（并入 P0 连续运行测试一起跑）。
- [ ] 已知限制：通知横幅上的应用名「TickerBar」由系统强制显示，无法按条隐藏；如需更彻底伪装可在打包时改应用显示名。

## 上轮小改进

- [x] 将自动行情刷新限制为最低 3 秒，降低免费接口压力。
- [x] 连续刷新失败时指数退避，成功后恢复正常频率。
- [x] 将尚未生效的“扩展时段”标记为待支持并禁用，避免误导。

完成于 2026-08-03，已补充前后端自动化测试。

## P0：日常使用稳定性

- [x] 股票切换时先验证新行情，再提交配置；失败时保留原股票与原配置。（2026-08-04：保存流程改为「先取新行情成功 → 再落盘」，失败不落盘并明确提示）
- [x] 区分开盘、休市、收盘和行情延迟，避免一直显示为实时状态。（2026-08-04：按交易时段 + 行情时间戳新鲜度推导 交易中/休市/延迟；节假日通过行情停留在往日识别，未内置交易所日历）
- [ ] 完成睡眠唤醒、断网重连、跨交易日和连续后台运行测试。
- [x] 为行情响应增加合理的大小限制和解析异常保护。（2026-08-04：响应体上限 64KB，超限报错不吞入）
- [x] 记录最近一次成功刷新时间与错误原因，便于排查偶发问题。（2026-08-04：记录在运行状态并挂在菜单栏 tooltip 上；设置页内展示归 P1）
- [ ] 观察腾讯免费行情接口的稳定性和限制，必要时增加备用数据源。

### P0 验收标准

- 连续运行 5 个交易日，无崩溃、无菜单栏消失、无明显资源泄漏。
- 网络恢复后无需重启，能够自动恢复行情。
- 无效股票、接口超时和异常响应不会覆盖最后一份可用配置。
- 休市和行情异常状态不会被误报成实时行情。

## P1：产品完整度

- [x] ~~实现扩展时段逻辑~~（2026-08-11 决定移除：A 股/港股无散户可用的扩展时段，开关永远不会生效。UI 开关与 `extendedHours` 配置字段一并删除；旧配置文件里的残留字段被 serde 忽略，无需迁移，已加测试守住）
- [x] ~~提供刷新频率设置~~（2026-08-05 决定不做：固定 3 秒刷新，不暴露设置项）
- [x] 增加“最后更新于”和当前重试状态的轻量提示。（2026-08-05：设置页预览卡下显示最后更新时间与上次错误，5 秒轮询；菜单栏 tooltip 同步保留）
- [x] 为配置结构增加正式迁移机制，升级后保留旧版本设置。（2026-08-05：v1→v2 迁移随多股票落地——解析层旧格式回退 + 启动时统一写回新格式，后续版本沿用此模式）
- [x] 完善空状态、首次启动引导和数据源说明。（2026-08-05：默认配置移除演示持仓，首启不再展示编造盈亏；首次启动自动打开设置窗并显示三步引导 + 数据源/免责说明，关闭后同会话不再出现）
- [ ] 根据实际使用反馈继续优化显示项管理与拖动体验。

### 审查遗留（2026-08-04 审查发现，同日全部完成）

- [x] App.vue 拆分：抽出 useStockSearch / usePointerDrag 组合式函数与 StockSection / PositionSection 组件，App.vue 由 1099 行降至约 700 行，useStockSearch 补齐独立单测。
- [x] 持仓数量/成本输入增加前端格式与范围校验，非法输入内联提示并阻止保存。
- [x] 配置文件权限收紧为 0600（写临时文件时即设置，含测试）。
- [x] User-Agent 版本号改为编译期取自 Cargo.toml。

## P2：对外分发

开源前体检（2026-08-11）：代码层无 CRITICAL/HIGH 问题；无硬编码密钥、无 XSS 面、Tauri 权限最小化、网络层有超时与 64KB 上限。以下为已处理与待决项。

- [x] 补齐 `.gitignore`（IDE 配置、签名材料、`.env`、`*.tsbuildinfo` 等），公开前防手滑。
- [x] 收紧 CSP：补 `object-src 'none'`、`frame-ancestors 'none'`、`base-uri 'self'`、`form-action 'none'`。
- [x] 补 LICENSE（MIT）与 Cargo.toml 元数据（description / license / repository）。
- [x] 依赖审计：`cargo audit` 唯一漏洞 RUSTSEC-2026-0235（rkyv）经 `cargo tree -i rkyv --target all` 核实**不在实际依赖图内**（rust_decimal 的未启用可选依赖），已在 `.cargo/audit.toml` 记录豁免与理由；GTK 系 unmaintained 警告同理仅存在于 Linux 目标。
- [x] Universal 二进制（Apple Silicon + Intel）+ dmg 打包，`pnpm release` 一条命令产出。
- [x] README 重写：安装门槛与 Gatekeeper 放行步骤、数据与隐私章节（本机存什么、出网发什么）、免责声明与接口使用说明。
- [ ] **Apple Developer ID 签名 + notarization（$99/年）** —— 唯一剩下的「陌生人能否顺滑安装」阻塞项，需用户决策是否付费。当前为 ad-hoc 签名，`spctl` 判定 rejected。
- [ ] 在干净系统上验证首次安装流程（需另一台机器或新用户账户）。
- [ ] 增加版本更新机制、发布说明和回滚方案（Tauri updater 需要签名密钥，与上一项绑定）。
- [ ] 在不同 macOS 版本与不同菜单栏空间下测试。

## 暂不计划

- 投资组合管理（总市值/总盈亏汇总、调仓记录）；多股票展示已于 2026-08-05 落地，见上文。
- 券商账户登录、自动读取持仓或交易功能。
- Windows、Linux 与移动端版本。

## 完成定义

只有同时满足以下条件，才从 Beta 标记为正式版：

1. P0 项全部完成并通过连续 5 个交易日验证。
2. 自动化测试与静态检查全部通过。
3. 对外分发时完成正式签名、notarization 与安装验证。
4. 已知限制在应用或文档中明确说明，不保留看似可用但实际无效的开关。

## 人工新增待办（2026-08-07 提出，同日全部完成）

- [x] 部分显示内容调整功能没有意义，例如市场状态，检查一下所有的显示内容现有的调整是否通用，哪些无需调整，哪些有特殊的调整
  - 建立显示项能力表（`metricCapability`），按指标性质只暴露适用的调整：
    - **纯文本**（股票代码/简称、市场状态、更新时间）：仅「短标签」，小数位/正负号/缩写全部隐藏并附文字说明。
    - **恒为正的价格**（当前价、昨收、最高/最低、平均成本）：仅小数位——正负号与箭头对永远为正的数没有意义。
    - **可正可负**（涨跌额、涨跌幅、持仓收益率、每股盈利）：小数位 + 正负号/箭头。
    - **可正可负且量级大**（持仓收益）：三项全适用。
    - **恒为正但量级大**（市值、持仓数量）：小数位 + 万/亿缩写，无正负号。
  - 摘要行同步只列适用项，不再对文本项显示「2 位 · 普通 · 不缩写」这种空话。
- [x] 启用收益计算这个开关没有意义 → 移除「启用持仓计算」开关。填数量与成本即计算，两项都清空即不计算；只填一项按「填漏了」拦下并提示。是否显示在菜单栏仍由「显示」页决定，两件事彻底解耦。
- [x] 收益计算支持多个股票 + 总收益
  - 持仓页改为「一屏列出全部股票」，每行含数量/成本输入与该股实时收益、收益率、市值，取代原来一次只能看一只的下拉选择器。
  - 新增 `portfolio` 模块与 `preview_portfolio` 命令：编辑中即时汇总（不必先保存）。
  - **多币种按币种分组，绝不相加**——没有汇率数据源，港币与人民币混加只会得出看似精确的错误数字；合计收益率按「合计收益 ÷ 合计成本」重算，而非各股收益率取平均。
  - 菜单栏下拉新增禁用态「合计」行；有持仓却暂无行情的股票会标注「N 只待更新」，不让不完整的数字冒充全部。
- [x] 除了标题尽量不要出现英文 → `QUOTE SOURCE`→「行情来源」、`ALERTS`→「提醒规则」、`LOCAL · PRIVATE`→「本地运行 · 不上传」；中文字距同步收窄（英文全大写的宽字距在中文下会散）。
- [x] 副标题展示不全且是英文 → 移除侧边栏 `MENU BAR UTILITY` 副标题。

### 第二批（2026-08-07 使用反馈，同日完成）

- [x] 改了设置无法撤销，只能关窗重开 → 顶栏新增「放弃修改」按钮 + 「有未保存的修改」提示。保存成功即刷新快照，按钮在无改动时禁用。
- [x] **回归修复**：持仓只填一项时把空字符串发给后端，弹出 `invalid value: string "", expected a Decimal` 这种 serde 英文报错。根因是上一批移除持仓开关后引入了「半填」中间态，而预览请求没设闸门。现在输入层非法一律不发 IPC，预览显示「待补全设置」并给中文提示。
- [x] 合计看不出怎么算的 → 合计块补上成本明细、「已计入 N / M 只」计数，以及计算口径说明：收益 = Σ(现价 − 成本) × 数量；收益率 = 合计收益 ÷ 合计成本（按仓位加权，非各股收益率平均）；多币种不换算；缺行情的股票单独说明。
- [x] 文案句号规范：单句不加句尾句号，多句连写时只在句间用句号、末句不带。含条件分支的说明（持仓合计）改为按需拼接后 `join('。')`，避免各分支各自处理标点。
- [x] 打开设置固定跳到「显示」→ 改为记住上次待过的分区（存 localStorage），首次打开落在「股票」，与首启引导落点一致。
- [x] 股票行样式与对齐 → 「菜单栏显示」标记移到股票名旁边（原先它与「移除」按钮同占末列，两者宽度差把代码列和箭头顶歪）；行改用 flex + 代码列定宽右对齐 + 操作列定宽；拖拽手柄收窄，排序箭头默认无边框、悬停整行才描边；行内说明改为「港币行情」这类中文短名，币种代码交给旁边的代码列。
- [x] 半填持仓的错误提示太丑 → 改为就地轻提示：出错的行描边、缺的那一格单独标出、右侧一句话说明；移除页面底部的红色横幅，顶栏也不再边填边报（只在真正点保存时报错）。
- [x] 无法确认覆盖安装是否生效 → 侧边栏底部原本写死的 `v0.1` 改为后端返回的真实版本 + 构建时间（取可执行文件修改时间，覆盖安装必变），形如 `v0.1.0 · 构建于 08-10 23:12`。
- [x] 股票列表只能选不能排序 → 复用显示项的拖拽 hook，股票行支持拖动手柄排序 + 上下箭头（键盘可用）。列表顺序即菜单栏下拉顺序；`moveStock` 保证「置顶」按股票身份跟随，不因下标位移串到别的股票。
- [x] 交易货币含义不明且是自由文本 → 改为下拉（人民币 CNY / 港币 HKD，与行情源覆盖的市场一致），附说明「添加股票时按市场自动识别，仅用于持仓收益的币种归类」；旧配置里的非常规币值保留为额外选项，不静默改写。